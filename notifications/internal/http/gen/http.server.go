// Package httpserver provides primitives to interact with the openapi HTTP API.
//
// Code generated by github.com/oapi-codegen/oapi-codegen/v2 version v2.7.1 DO NOT EDIT.
package httpserver

import (
	"fmt"
	"net/http"
	"time"

	"github.com/labstack/echo/v4"
	"github.com/oapi-codegen/runtime"
)

// Defines values for CIReportStatus.
const (
	Crashed CIReportStatus = "crashed"
	Failed  CIReportStatus = "failed"
	Passed  CIReportStatus = "passed"
	Pending CIReportStatus = "pending"
	Timeout CIReportStatus = "timeout"
)

// Valid indicates whether the value is a known member of the CIReportStatus enum.
func (e CIReportStatus) Valid() bool {
	switch e {
	case Crashed:
		return true
	case Failed:
		return true
	case Passed:
		return true
	case Pending:
		return true
	case Timeout:
		return true
	default:
		return false
	}
}

// Defines values for CIStepStatus.
const (
	Failure CIStepStatus = "failure"
	Skipped CIStepStatus = "skipped"
	Success CIStepStatus = "success"
)

// Valid indicates whether the value is a known member of the CIStepStatus enum.
func (e CIStepStatus) Valid() bool {
	switch e {
	case Failure:
		return true
	case Skipped:
		return true
	case Success:
		return true
	default:
		return false
	}
}

// Defines values for ErrorCode.
const (
	EXPIREDAUTHTOKEN ErrorCode = "EXPIRED_AUTH_TOKEN"
	INTERNALERROR    ErrorCode = "INTERNAL_ERROR"
	INVALIDAUTHTOKEN ErrorCode = "INVALID_AUTH_TOKEN"
	INVALIDJSON      ErrorCode = "INVALID_JSON"
	MISSINGAUTHTOKEN ErrorCode = "MISSING_AUTH_TOKEN"
	NOTFOUND         ErrorCode = "NOT_FOUND"
	VALIDATIONERROR  ErrorCode = "VALIDATION_ERROR"
)

// Valid indicates whether the value is a known member of the ErrorCode enum.
func (e ErrorCode) Valid() bool {
	switch e {
	case EXPIREDAUTHTOKEN:
		return true
	case INTERNALERROR:
		return true
	case INVALIDAUTHTOKEN:
		return true
	case INVALIDJSON:
		return true
	case MISSINGAUTHTOKEN:
		return true
	case NOTFOUND:
		return true
	case VALIDATIONERROR:
		return true
	default:
		return false
	}
}

// Defines values for WebhookEvent.
const (
	CiFinished WebhookEvent = "ci_finished"
	CiStarted  WebhookEvent = "ci_started"
)

// Valid indicates whether the value is a known member of the WebhookEvent enum.
func (e WebhookEvent) Valid() bool {
	switch e {
	case CiFinished:
		return true
	case CiStarted:
		return true
	default:
		return false
	}
}

// CIReportResponse defines model for CIReportResponse.
type CIReportResponse struct {
	Commit    string         `json:"commit"`
	CreatedAt time.Time      `json:"created_at"`
	ExitCode  int32          `json:"exit_code"`
	Stage     string         `json:"stage"`
	Status    CIReportStatus `json:"status"`
	Stderr    string         `json:"stderr"`
	Stdout    string         `json:"stdout"`
	Steps     []CIReportStep `json:"steps"`
	Uid       string         `json:"uid"`
}

// CIReportStatus defines model for CIReportStatus.
type CIReportStatus string

// CIReportStep defines model for CIReportStep.
type CIReportStep struct {
	DurationMs int32        `json:"duration_ms"`
	Name       string       `json:"name"`
	Status     CIStepStatus `json:"status"`
}

// CIStepStatus defines model for CIStepStatus.
type CIStepStatus string

// ErrorCode defines model for ErrorCode.
type ErrorCode string

// ErrorResponse defines model for ErrorResponse.
type ErrorResponse struct {
	Code    ErrorCode `json:"code"`
	Message string    `json:"message"`
}

// MessageResponse defines model for MessageResponse.
type MessageResponse struct {
	Message string `json:"message"`
}

// WebhookEvent defines model for WebhookEvent.
type WebhookEvent string

// WebhookRequest defines model for WebhookRequest.
type WebhookRequest struct {
	Commit   string       `json:"commit"`
	Event    WebhookEvent `json:"event"`
	ExitCode *int32       `json:"exit_code,omitempty"`
	Stage    *string      `json:"stage,omitempty"`
	Stderr   *string      `json:"stderr,omitempty"`
	Stdout   *string      `json:"stdout,omitempty"`
	Uid      string       `json:"uid"`
	VmId     *string      `json:"vm_id,omitempty"`
}

// WebhookReceiveJSONRequestBody defines body for WebhookReceive for application/json ContentType.
type WebhookReceiveJSONRequestBody = WebhookRequest

// ServerInterface represents all server handlers.
type ServerInterface interface {

	// (GET /reports/{uid})
	ReportsGet(ctx echo.Context, uid string) error

	// (POST /webhook)
	WebhookReceive(ctx echo.Context) error
}

// ServerInterfaceWrapper converts echo contexts to parameters.
type ServerInterfaceWrapper struct {
	Handler ServerInterface
}

// ReportsGet converts echo context to params.
func (w *ServerInterfaceWrapper) ReportsGet(ctx echo.Context) error {
	var err error
	// ------------- Path parameter "uid" -------------
	var uid string

	err = runtime.BindStyledParameterWithOptions("simple", "uid", ctx.Param("uid"), &uid, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationPath, Explode: false, Required: true, Type: "string", Format: ""})
	if err != nil {
		return echo.NewHTTPError(http.StatusBadRequest, fmt.Sprintf("Invalid format for parameter uid: %s", err))
	}

	// Invoke the callback with all the unmarshaled arguments
	err = w.Handler.ReportsGet(ctx, uid)
	return err
}

// WebhookReceive converts echo context to params.
func (w *ServerInterfaceWrapper) WebhookReceive(ctx echo.Context) error {
	var err error

	// Invoke the callback with all the unmarshaled arguments
	err = w.Handler.WebhookReceive(ctx)
	return err
}

// This is a simple interface which specifies echo.Route addition functions which
// are present on both echo.Echo and echo.Group, since we want to allow using
// either of them for path registration
type EchoRouter interface {
	CONNECT(path string, h echo.HandlerFunc, m ...echo.MiddlewareFunc) *echo.Route
	DELETE(path string, h echo.HandlerFunc, m ...echo.MiddlewareFunc) *echo.Route
	GET(path string, h echo.HandlerFunc, m ...echo.MiddlewareFunc) *echo.Route
	HEAD(path string, h echo.HandlerFunc, m ...echo.MiddlewareFunc) *echo.Route
	OPTIONS(path string, h echo.HandlerFunc, m ...echo.MiddlewareFunc) *echo.Route
	PATCH(path string, h echo.HandlerFunc, m ...echo.MiddlewareFunc) *echo.Route
	POST(path string, h echo.HandlerFunc, m ...echo.MiddlewareFunc) *echo.Route
	PUT(path string, h echo.HandlerFunc, m ...echo.MiddlewareFunc) *echo.Route
	TRACE(path string, h echo.HandlerFunc, m ...echo.MiddlewareFunc) *echo.Route
}

// RegisterHandlersOptions configures RegisterHandlersWithOptions.
type RegisterHandlersOptions struct {
	// BaseURL is prepended to every registered path so the API can be served
	// under a prefix.
	BaseURL string
	// OperationMiddlewares lets the caller attach per-operation middleware at
	// registration time. The map key is the OpenAPI `operationId` value as it
	// appears in the spec (the raw, un-normalized form). Operations that have
	// no entry are registered with no extra middleware. A nil map disables
	// per-operation middleware entirely.
	OperationMiddlewares map[string][]echo.MiddlewareFunc
}

// RegisterHandlers adds each server route to the EchoRouter.
func RegisterHandlers(router EchoRouter, si ServerInterface) {
	RegisterHandlersWithOptions(router, si, RegisterHandlersOptions{})
}

// RegisterHandlersWithBaseURL registers handlers and prepends BaseURL to the
// paths so the API can be served under a prefix.
func RegisterHandlersWithBaseURL(router EchoRouter, si ServerInterface, baseURL string) {
	RegisterHandlersWithOptions(router, si, RegisterHandlersOptions{BaseURL: baseURL})
}

// RegisterHandlersWithOptions registers handlers using the supplied options,
// including any per-operation middleware.
func RegisterHandlersWithOptions(router EchoRouter, si ServerInterface, options RegisterHandlersOptions) {

	wrapper := ServerInterfaceWrapper{
		Handler: si,
	}

	router.GET(options.BaseURL+"/reports/:uid", wrapper.ReportsGet, options.OperationMiddlewares["reports.get"]...)
	router.POST(options.BaseURL+"/webhook", wrapper.WebhookReceive, options.OperationMiddlewares["webhook.receive"]...)

}
